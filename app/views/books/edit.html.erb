<h3>New book</h3>
<% if @book.errors.any? %>
	ul{<% @book.errors.full_messages.each do |message| %>}
<%= form_for(@book) do |f| %>

	<%= f.label :Title %><br/>
	<%= f.text_field :title %>

	<h4>Body</h4>
	<%= f.text_area :body %>

	<br><%= f.submit 'Create Book' %></br>




